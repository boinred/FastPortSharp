# Archive Index — 2026-05

Completed PDCA cycles archived in May 2026.

| Cycle | Match Rate | Final Status | Folder |
|---|:-:|---|---|
| `game-server-template-from-network-engine` | 100% | LibCommons/LibNetworks를 네트워크 엔진으로, FastPortGameServerTemplate 신규 starter 프로젝트 추가. SampleClient로 echo round-trip 검증. | [./game-server-template-from-network-engine/](./game-server-template-from-network-engine/) |
| `game-server-template-scaffold-scripts` | 100% | Cross-platform `scripts/scaffold-game-server.{sh,ps1}` + 7 golden cases + 3-OS CI matrix. 5분 → 10초 부트스트랩(30배 단축). | [./game-server-template-scaffold-scripts/](./game-server-template-scaffold-scripts/) |
| `fix-base-session-send-fifo-test-flakiness` | 100% | `BaseSessionSendPolicyTests` 두 메서드의 race를 black-box `BatchedFifoObserver`로 제거. Production 0줄 변경. macOS GHA 5/5 + 로컬 50/50. | [./fix-base-session-send-fifo-test-flakiness/](./fix-base-session-send-fifo-test-flakiness/) |
| `fix-server-telemetry-export-jsonl-flush-flakiness` | 95-98% | `ServerTelemetryExport` test의 windows flake. 진짜 원인은 reader `FileShare.Read` mismatch. Reader fix + production hardening (WriteThrough, FileShare.ReadWrite, Math.Max(0.05) clamp). 3-OS × 5 = 15/15 PASS. | [./fix-server-telemetry-export-jsonl-flush-flakiness/](./fix-server-telemetry-export-jsonl-flush-flakiness/) |
| `move-test-projects-to-testprojects-folder` | 97% | 5 test 프로젝트(`FastPortTests`, `FastPortTestLoadRunner`, `FastPortTestLoadValidation`, `FastPortTestSmokeServer`, `LibTestTelemetry`)를 `tests-projects/`로 일괄 이동. Production 0줄 변경. Sanity review로 README/HANDOFF gap 19건 발견·수정 (lesson: head truncation 주의). | [./move-test-projects-to-testprojects-folder/](./move-test-projects-to-testprojects-folder/) |
| `maui-telemetry-dashboard-foundation` | 95-97.75% | `FastPortDashboard.Maui` (macOS Catalyst + Windows desktop) + 별도 `FastPortSharp.Dashboard.sln`로 build.yml CI 격리. MVVM + LiveCharts2 + IPollingAdapter (Mock/Jsonl) + 6 KPI + JSONL polling. 직전 cycle의 FileShare.ReadWrite lesson 재활용. RTT chart는 follow-up cycle로 분리. | [./maui-telemetry-dashboard-foundation/](./maui-telemetry-dashboard-foundation/) |
| `dashboard-mvvm-toolkit-migration` | 96% | `DashboardViewModel`을 `CommunityToolkit.Mvvm` 8.4 source generator로 migration. `[ObservableProperty]` × 11 + `[RelayCommand]` × 2 + `[NotifyCanExecuteChangedFor]` × 2. Manual INPC/Command 0건 잔존. LOC 193→118 (~39%, boilerplate 자체는 ~80%↓). XAML binding 무변경. 단일 commit `a82c25c`. Lesson: Plan LOC 추정 시 boilerplate와 비즈니스 로직 분리 필요. | [./dashboard-mvvm-toolkit-migration/](./dashboard-mvvm-toolkit-migration/) |
| `dashboard-unit-tests` | 96.2% | `tests-projects/FastPortDashboardTests` (MSTest) 신규, Dashboard sln only. 18 tests (ViewModel 10 + Mock 3 + Jsonl 5), 745ms. Production 변경 0 + CI 무변경 + 139 회귀 0. Design Option A (maccatalyst TFM 매칭) → Option C (`<Compile Include Link>` pure net10.0) 전환: Resizetizer 출력 충돌 + `dotnet test`가 maccatalyst Exe 미실행. 단일 commit `4184d57`. Lesson: MAUI test project TFM gotcha (maui-test-project-tfm-gotcha 후보). | [./dashboard-unit-tests/](./dashboard-unit-tests/) |
| `extract-LibDashboardCore` | 98.5% | ViewModel/Adapter 6 파일을 `FastPortDashboard.Core` (net10.0 Class Library)로 추출. 6 git mv (rename detection 100%, content 0). Namespace 보존(`FastPortDashboard.Maui.*`) + csproj `<RootNamespace>`. 직전 cycle의 `<Compile Include>` 6줄 제거 → ProjectReference 1줄로 대체. Toolkit + LibTestTelemetry transitive. Maui csproj -7, Test csproj -15 줄. MainPage.xaml `;assembly=FastPortDashboard.Core` 한정자 1줄 추가. Dashboard 빌드 0/0 + 18 tests pass (732ms) + FastPortSharp.sln 회귀 0 (139 tests). 단일 commit `319dc3b`. Lesson: maui-xaml-assembly-qualifier-gotcha. | [./extract-LibDashboardCore/](./extract-LibDashboardCore/) |
| `dashboard-rtt-chart` | 100% | Microcharts.Maui 1.0.1로 Client RTT P95Ms LineChart 추가. `ClientRttSeries` + `ApplyClientSnapshot` + ApplySnapshot null-safe Server/Client 분기 (기존 18 tests 회귀 0). MockPollingAdapter `Combined(client, server)` yield (RTT 50~100ms random walk). MainPage.xaml chart placeholder → `<microcharts:ChartView>`, code-behind에서 `CollectionChanged` → `LineChart` 변환 (Core lib Microcharts 의존 0 격리). Tests +2 (20 총). Dashboard 빌드 0/0 + 20 tests pass (734ms) + FastPortSharp.sln 회귀 0. Production LibTestTelemetry 변경 0 (RTT 필드 contract 이미 존재). 단일 commit `84f725e`. Pattern: 첫 visual chart + code-behind UI lib 격리 reference. | [./dashboard-rtt-chart/](./dashboard-rtt-chart/) |
| `dashboard-throughput-chart` | 100% | RTT chart 아래 Server Throughput (B/s) LineChart 수직 스택 추가 (`#4CAF50` green). MainPage.xaml.cs `UpdateThroughputChart` (UpdateRttChart mirror) + `ThroughputSeries.CollectionChanged` 구독. **ViewModel/Core/Tests 변경 0줄** — ThroughputSeries는 Foundation cycle부터 채워지고 있어 데이터 layer 변경 불필요. Mirror pattern으로 직전 RTT cycle의 code-behind reference 재사용성 검증 완료 (< 8 turn). Dashboard 빌드 0/0 + 20 tests 회귀 0 (735ms) + FastPortSharp.sln 회귀 0. 단일 commit `d287456`. Lesson: data layer 사전 완비 시 N번째 chart 추가는 narrow cycle (UI 2 파일만). | [./dashboard-throughput-chart/](./dashboard-throughput-chart/) |
| `dashboard-jsonl-offset-fix` | 100% | `JsonlPollingAdapter.StreamAsync`의 yield-resume gap race 정식 수정. `ReadNewSnapshotsAsync` 시그너처를 `(Snapshots, NewOffset)` tuple로 변경, `StreamAsync`가 read 직후 offset 확정 → yield → delay. `fileLength`는 file open 직후 stable snapshot으로 capture (race window 최소화). Public API (`IAsyncEnumerable<...> StreamAsync(CancellationToken)`) 변경 0, consumer 영향 0. Truncation 로직 + `FileShare.ReadWrite \| FileShare.Delete` 보존. Test 변경 0 — 기존 백그라운드 writer 패턴 5/5 그대로 통과 (race-safe 시나리오 재검증). Dashboard 빌드 0/0 + 20 tests 회귀 0 (731ms) + FastPortSharp.sln 회귀 0. 단일 commit `197072a`. Lesson: 백그라운드 writer test가 race 회피용에서 race-safe 검증으로 역할 전환. | [./dashboard-jsonl-offset-fix/](./dashboard-jsonl-offset-fix/) |
| `dashboard-ci-add` | 100% | `.github/workflows/dashboard.yml` 신규 — Dashboard sln 전용 CI workflow. macOS + Windows 2-OS matrix (Linux는 MAUI/maccatalyst 불가로 제외), builds.release branch + Dashboard 관련 6 path filter entries로 trigger 격리 (메인 build.yml 영향 0). Steps: Force LF (Win) → checkout → setup .NET 10 → `dotnet workload install maui` → restore/build/test FastPortSharp.Dashboard.sln. fail-fast: false. **build.yml + scaffold.yml + 소스 코드 + 모든 sln git diff 0** — 완전 격리. 단일 commit `8c8be15`. Lesson: 격리 정책 일관성 — sln (Foundation) → ProjectReference (extract-LibDashboardCore) → CI workflow (본 cycle). | [./dashboard-ci-add/](./dashboard-ci-add/) |
| `dashboard-multi-rtt-overlay` | 100% | 직전 cycle의 단일 P95 LineChart를 P50/P95/P99 3-line overlay로 확장. Microcharts 단일 series 한계를 `SKCanvasView` direct Skia drawing으로 우회 (LiveCharts2 crash 회피). 신규 `TimedRttPoint` readonly record struct (TimestampUnixMs/P50Ms/P95Ms/P99Ms), `ClientRttSeries`가 단일 collection으로 3 percentile 표현. Code-behind `OnRttCanvasPaintSurface`에서 직접 draw + 우상단 legend, Y축 자동 스케일 `max(P99) × 1.1`. Color: P50 blue / P95 orange / P99 red (Material warning gradient). TimedDoublePoint는 Throughput series가 그대로 사용 → 보존, Throughput chart 동작 변경 0. Tests T-VM-11/12 갱신 (TimedRttPoint API). Dashboard 빌드 0/0 + 20 tests 회귀 0 (755ms) + FastPortSharp.sln 회귀 0. 단일 commit `cbca03b`. Lesson: code-behind UI lib 격리 패턴 4번째 cycle 적용으로 reference reusability 입증. | [./dashboard-multi-rtt-overlay/](./dashboard-multi-rtt-overlay/) |
| `dashboard-revert-skcanvasview-keep-data` | 100% structural, Critical SC-7 Not Met | 가설 ("SKCanvasView가 macOS 26 SwiftUI crash trigger") 검증을 위한 UI revert cycle. MainPage.xaml의 `skia:SKCanvasView` → `microcharts:ChartView` 복귀, MainPage.xaml.cs `OnRttCanvasPaintSurface` 제거 + `UpdateRttChart` 복원 (P95 단일). TimedRttPoint + ViewModel + Tests 변경 0 (data layer 보존). Dashboard 빌드 0/0 + 20 tests 회귀 0 (743ms) + FastPortSharp.sln 회귀 0. **그러나 Debug 빌드 실행 시 동일 SIGSEGV 재현 → 가설 반증 확정**. Crash는 SKCanvasView/특정 UI lib와 무관한 baseline UIKit-on-Catalyst 이슈. Memory `maccatalyst-26-swiftui-observation-release-crash` Debug도 영향으로 정정. 단일 commit `667ca17`. Lesson: 짧은 timeout 1회 관찰("Debug 정상")의 신뢰성 한계 + verification 경로를 UI 실행에서 headless E2E로 전환 필요. | [./dashboard-revert-skcanvasview-keep-data/](./dashboard-revert-skcanvasview-keep-data/) |
| `dashboard-e2e-mock-tests` | 100% | UI 실행 검증 차단된 macOS 26 환경에서 비즈니스 로직 headless E2E verification 경로 신규. `tests-projects/FastPortDashboardTests/E2E/MockE2ETests.cs` 5 tests: full pipeline / RTT P50P95P99 percentiles / cancellation graceful / KPI monotonic / lifecycle (ConnectCommand → Polling → DisconnectCommand → Disconnected). `PumpMockForAsync` helper로 cancellation + adapter 셋업 표준화 (30ms interval × 250ms duration). Production/ViewModel/Adapter 변경 0. Dashboard 빌드 0/0 + **25 tests 모두 pass (1s)** + FastPortSharp.sln 회귀 0. dashboard.yml CI path filter (`tests-projects/FastPortDashboardTests/**`)에 자동 포함 → CI 변경 0. 단일 commit `e6474c1`. Lesson: UI 차단 환경에서도 ViewModel + Adapter API가 cancellation-friendly하면 1초 안에 풀 pipeline 검증 가능. | [./dashboard-e2e-mock-tests/](./dashboard-e2e-mock-tests/) |
| `dashboard-chart-multi-rtt-overlay-v2` | 95% | SkiaSharp 회피책 위에서 P50/P95/P99 multi-line overlay 복원. 신규 `MultiLineChartDrawable : IDrawable` (99 lines) + `LineChartSeries` record (8) + Core `LineChartMath.ComputeRangeMulti` (+38). 기존 `LineChartDrawable`은 **0줄 변경** (Throughput용으로 보존) — Option B 분리 신규 패턴으로 회귀 위험 0. RTT 차트 3-line (P50 #2196F3 / P95 #FF9800 / P99 #F44336) + drawable 내부 우상단 simple legend. Y축은 `ComputeRangeMulti`로 통합 자동 스케일. Tests 37/37 (32 기존 + 5 신규). Build warning 0. SkiaSharp/Microcharts using 0건. Lesson: 검증된 single 클래스에 분기 추가하기보다 분리 신규 패턴이 회귀 안전성 100%. | [./dashboard-chart-multi-rtt-overlay-v2/](./dashboard-chart-multi-rtt-overlay-v2/) |
| `dashboard-chart-graphicsview-migration` | 95% (manual confirmed) | **SkiaSharp 가설 입증 + 회피책 확정 cycle**. 직전 `dashboard-revert-skcanvasview-keep-data`에서 ChartView 복귀로도 crash 재현되었던 이유 = `microcharts:ChartView` 내부가 여전히 Skia. 본 cycle은 `Microcharts.Maui` PackageReference + `MauiProgram.UseMicrocharts()` + `using Microcharts/SkiaSharp` 일체 제거. 차트는 `Microsoft.Maui.Graphics` `GraphicsView` + 자작 `LineChartDrawable : IDrawable` (78 lines)로 재구현, 순수 수학 `LineChartMath` (`ComputeRange`/`ComputeStepX`)을 Core로 분리하여 net10.0 unit test 7개 추가. 시각화 parity 유지 (RTT P95 #FF9800, Throughput #4CAF50). Dashboard 빌드 0/0 + **32 tests pass (1s, 신규 7 + 기존 25)** + macOS Catalyst Debug 차트 정상 렌더 + crash 0건 (사용자 manual confirm). Memory `maccatalyst-26-swiftui-observation-release-crash`를 "Apple framework 자체 버그 / 회피 불가"에서 "SkiaSharp 완전 제거 = 검증된 회피책"으로 격상 정정. Lesson: 부분 회피 시도(SKCanvasView↔ChartView swap)는 의미 없음 — PackageReference 차원에서 의존성 완전 제거해야 효과. | [./dashboard-chart-graphicsview-migration/](./dashboard-chart-graphicsview-migration/) |
| `template-contracts-extraction` | 95% | **Exe-only proto 안티패턴 해소 + 리포 구조 그룹화 cycle**. `FastPortGameServerTemplate` (Exe) 내부에 갇혀 있던 `Sample.proto` + `PacketIds.cs`를 신규 `FastPortGameServerTemplate.Contracts` (net10.0 Class Library)로 분리하고, 3개 template 프로젝트(`Template`/`Contracts`/`SampleClient`)를 `template-projects/` 폴더로 일괄 그룹화 (`tests-projects/` 대칭). 2-Phase Pragmatic 실행 (`b3e4e2c` folder move + `ff105ea` Contracts extraction). cs/proto 파일 byte 변경 0 (rename only) + namespace 보존으로 downstream 0 변경. SampleClient bin에서 `FastPortGameServerTemplate.dll/exe` 부재 확인 (Exe→Exe 안티패턴 해소). RTT 16.174ms (Phase 1) / 17.056ms (Phase 2) round-trip 검증. csproj ProjectReference depth `..\` → `..\..\` 갱신, sln 자동 Solution Folder `template-projects` 생성 (bonus). `dashboard-echo-client-tab` cycle unblock + `tests-projects/`↔`template-projects/` 리포 대칭 확보. Lesson: pure refactor에도 build+round-trip+diff-zero 3종 검증 셋트 필수. | [./template-contracts-extraction/](./template-contracts-extraction/) |
| `template-contracts-scaffold-fix` | 100% | **직전 cycle regression 폐쇄 cycle**. `template-contracts-extraction`이 Contracts lib 도입 후 scaffold script 미갱신으로 외부 사용자 출력이 컴파일 불가 상태였던 문제 완전 해소. `scaffold-game-server.{sh,ps1}` 양쪽에 `CONTRACTS_SRC` + `copy_contracts` 함수 + `replace_tokens` Template/Contracts subtree iteration + sln 4-project 등록 추가 (Option C Pragmatic — 별도 함수 + 패턴 답습). step counter `[1/12]~[12/12]` 보존으로 기존 stdout-contains fixture 4개 case 비손상. 7-case golden file diff sh 7/7 + ps1 7/7 PASS, sh/ps1 byte-identical 유지. case-01 fixture는 `run.sh --update-golden` 자동 재생성, case-05/07 files-present.txt +3 Contracts entries 수동 추가, case-02/03/04/06 영향 0. 계획 외 발견: source `template-projects/` 깊이 2 vs scaffold output flat 구조 불일치로 `..\..\LibCommons` → `..\LibCommons` path-fix 자동 변환 추가 (sed + PowerShell Replace 양쪽). 단일 atomic commit `241cef2` (10 files, +852/-53). scaffold output `dotnet build` 0/0 + 내부 `[12/12]` smoke 통과. Lesson: regression 폐쇄 cycle의 가치 + 계획 외 발견(path depth)을 follow-up으로 미루지 않고 Do 단계에 즉시 흡수. | [./template-contracts-scaffold-fix/](./template-contracts-scaffold-fix/) |

## Conventions

- Each cycle folder contains the full PDCA document chain: `*.prd.md` → `*.plan.md` → `*.design.md` → `*.analysis.md` → `*.report.md`.
- Archives are immutable references — corrections happen in a new cycle, not by editing archived files.
