# External Protos destination

Copies shared proto files outside the scaffold repository and verifies the
generated project still passes its smoke build through the rewritten relative
Protobuf include path.
