# case-07-no-git-no-smoke

Same as case-01 but emphasises that `--no-git` produces no `.git/`
and `--skip-smoke` skips the dotnet build step. Distinct project
name (`Foo`) so it does not overlap with case-01's golden baseline.

Design Ref: §8.3 case-07.
